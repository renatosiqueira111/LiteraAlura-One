package com.example.ChallengeLiteraAlura.ONE.repository;

import com.example.ChallengeLiteraAlura.ONE.modelos.DadosAutor;
import org.springframework.data.jpa.repository.JpaRepository;

public interface DadosRepositoryAutor extends JpaRepository<DadosAutor, Long> {
}
